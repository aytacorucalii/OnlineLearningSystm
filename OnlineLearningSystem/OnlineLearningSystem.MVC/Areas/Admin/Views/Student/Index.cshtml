@model IEnumerable<StudentListItemDTO>

<div class="container-fluid">
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Tələbələr</h6>
        </div>
        <div class="d-flex justify-content-end px-3 mt-3">
            <a class="btn btn-primary" style="width: 160px" asp-controller="Student" asp-action="Create">+ Tələbə Əlavə Et</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" width="100%" cellspacing="0" id="studentTable">
                    <thead class="thead-dark">
                        <tr>

                            <th>#</th>
                            <th>Ad</th>
                            <th>Soyad</th>
                            <th>Əməliyyatlar</th>
                        </tr>
                    </>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.Name</td>
                                <td>@item.Surname</td>
                                <td>
                                    <a class="btn btn-primary" asp-controller="Student" asp-action="Update" asp-route-id="@item.Id">Yenilə</a>
                                    <a class="btn btn-danger" asp-controller="Student" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                                    <a class="btn btn-info" asp-controller="Student" asp-action="Details" asp-route-id="@item.Id">Detallar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="~/admin/vendor/datatables/jquery.dataTables.min.js" defer></script>
<script src="~/admin/vendor/datatables/dataTables.bootstrap4.min.js" defer></script>

<script src="~/admin/js/demo/datatables-demo.js" defer></script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        $('#studentTable').DataTable();  // Initialize DataTables after the DOM is loaded
    });
</script>
